image: python:3.12-slim

clone:
  depth: 1

definitions:
  caches:
    ci-venv:
      key:
        files:
          - requirements-ci.txt
          - src/pyproject.toml
      path: .venv

  steps:
    - step: &run-tests
        name: "Run Tests"
        caches:
          - ci-venv
        script:
          - test -x .venv/bin/python || python3 -m venv .venv
          - export PATH="$PWD/.venv/bin:$PATH"
          - python3 -c "import anthropic, openai, httpx, yaml, tomli_w, pytest" 2>/dev/null || pip install --quiet -r requirements-ci.txt
          - python3 -c "import revue" 2>/dev/null || pip install --quiet --no-deps -e src/
          - cd src && PYTHONPATH=$(pwd) python3 -m pytest revue/tests/ -q --log-cli-level=WARNING

    - step: &revue-review
        name: "Revue.io AI Code Review"
        image: python:3.12
        caches:
          - ci-venv
        script:
          - test -x .venv/bin/python || python3 -m venv .venv
          - export PATH="$PWD/.venv/bin:$PATH"
          - python3 -c "import anthropic, openai, httpx, yaml, tomli_w, pytest" 2>/dev/null || pip install --quiet -r requirements-ci.txt
          - python3 -c "import revue" 2>/dev/null || pip install --quiet --no-deps -e src/
          - git diff origin/${BITBUCKET_PR_DESTINATION_BRANCH}...HEAD > /tmp/revue_pr.diff
          - echo "[revue] Diff size $(wc -l < /tmp/revue_pr.diff) lines"
          - echo "[revue] Fetching PR description for context filtering..."
          - |
            curl -sf "https://api.bitbucket.org/2.0/repositories/${BITBUCKET_WORKSPACE}/${BITBUCKET_REPO_SLUG}/pullrequests/${BITBUCKET_PR_ID}" \
              -u "${BITBUCKET_USERNAME}:${BITBUCKET_API_TOKEN}" \
              | python3 -c "import json,sys; print(json.load(sys.stdin).get('description',''))" \
              > /tmp/revue_pr_description.txt \
            && echo "[revue] PR description fetched ($(wc -c < /tmp/revue_pr_description.txt) bytes)" \
            || echo "[revue] PR description fetch failed — continuing without context"
          - echo "[revue] Validating config..."
          - PYTHONPATH="$(pwd)/src" python3 -m revue.cli validate --config .revue.yml
          - echo "[revue] Starting AI code review..."
          - export PYTHONPATH="$(pwd)/src"
          - export APP_ENV=staging  # Enable REVUE_TIER_OVERRIDE for CI
          - export REVUE_METRICS_ENABLED=1
          # Provider and model are read from .revue.yml — no --provider/--model flags needed.
          # Set the API key CI variable matching your configured provider:
          #   anthropic → ANTHROPIC_API_KEY   openai → OPENAI_API_KEY  (REVUE-148)
          - python3 -u src/revue/cli.py review --diff /tmp/revue_pr.diff --platform bitbucket --pr-id "$BITBUCKET_PR_ID" --workspace "$BITBUCKET_WORKSPACE" --repo-slug "$BITBUCKET_REPO_SLUG" --bb-username "$BITBUCKET_USERNAME" --bb-token "$BITBUCKET_API_TOKEN" --config .revue.yml --comment-style per-issue --pr-description-file /tmp/revue_pr_description.txt --verbose
          - echo "[revue] Review complete"
        artifacts:
          - .revue/metrics.jsonl

pipelines:
  branches:
    main:
      - step: *run-tests
      - step:
          name: "Build Linux x86_64"
          image: python:3.12-slim
          caches:
            - pip
          script:
            - apt-get update && apt-get install -y --no-install-recommends gcc patchelf libc6-dev
            - pip install nuitka ordered-set zstandard
            - pip install -e src/
            - python -u build/build_nuitka.py
            - python -u build/build_wheel.py
          artifacts:
            - dist/wheels/*.whl
      - step:
          name: "Build macOS ARM64"
          runs-on:
            - self.hosted
            - macos
          script:
            - python3.12 -m venv .build-venv
            - .build-venv/bin/pip install nuitka ordered-set zstandard
            - .build-venv/bin/pip install -e src/
            - .build-venv/bin/python -u build/build_nuitka.py
            - .build-venv/bin/python -u build/build_wheel.py
          artifacts:
            - dist/wheels/*.whl
      - step:
          name: "Collect Artifacts"
          script:
            - ls -la dist/wheels/ 2>/dev/null || echo "No wheels built (self-hosted runners may be offline)"
          artifacts:
            - dist/wheels/*.whl
      - parallel:
          steps:
            - step:
                name: "Deploy Web → Fly.io"
                script:
                  - curl -L https://fly.io/install.sh | sh
                  - export PATH="$HOME/.fly/bin:$PATH"
                  # FLY_APP defaults to revue-io; change to the new app name after domain purchase.
                  - flyctl deploy --app "${FLY_APP:-revue-io}" --config src/web/fly.toml --remote-only
            - step:
                name: "Publish CLI → PyPI"
                script:
                  - pip install --quiet twine
                  - |
                    if ls dist/wheels/*.whl 2>/dev/null; then
                      export TWINE_USERNAME=__token__
                      export TWINE_PASSWORD=$PYPI_API_TOKEN
                      python -m twine upload --non-interactive dist/wheels/*.whl
                    else
                      echo "No wheels found — skipping PyPI upload"
                    fi
    develop:
      - step: *run-tests

  pull-requests:
    "**":
      - step: *run-tests
      - step: *revue-review
