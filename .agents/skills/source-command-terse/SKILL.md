---
name: "source-command-terse"
description: "Run the migrated source command `terse`."
---

# source-command-terse

Use this skill when the user asks to run the migrated source command `terse`.

## Command Template

# Compact output mode

Response style: terse, results-focused. No explanations, no context, no reasoning. Just the outcome.

Use this when you need quick answers without elaboration.

## MANUAL MIGRATION REQUIRED

Migrated from source command `terse` into a Codex skill. Invoke it as `$source-command-terse` and manually rewrite any slash-command behavior that depended on provider-specific runtime expansion.
