---
name: "source-command-verbose"
description: "Run the migrated source command `verbose`."
---

# source-command-verbose

Use this skill when the user asks to run the migrated source command `verbose`.

## Command Template

# Verbose output mode

Response style: detailed, explanatory. Include context, reasoning, alternatives considered, and implications.

Use this when you need full understanding of the decision or recommendation.

## MANUAL MIGRATION REQUIRED

Migrated from source command `verbose` into a Codex skill. Invoke it as `$source-command-verbose` and manually rewrite any slash-command behavior that depended on provider-specific runtime expansion.
