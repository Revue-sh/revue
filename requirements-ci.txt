openai>=1.0
anthropic>=0.20
httpx>=0.27
pyyaml>=6.0
tomli-w>=1.0
pytest
# REVUE-405: pin the build backend + PEP 660 editable-build helper so the three
# editable installs run with --no-build-isolation against the cached .venv,
# instead of fetching hatchling from PyPI on every run (which bricked #1026-1028).
hatchling>=1.21
editables
